{
  "name": "voipc-client",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "build:wasm": "cd ../crates/voipc-web && wasm-pack build --target web --release --out-dir ../../client/src/lib/wasm --out-name voipc_web",
    "build:web": "npm run build:wasm && vite build --mode web",
    "dev:web": "vite --mode web"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-notification": "^2.4.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5",
    "@tauri-apps/cli": "^2",
    "svelte": "^5",
    "typescript": "^5",
    "vite": "^6",
    "wasm-pack": "^0.13"
  }
}
