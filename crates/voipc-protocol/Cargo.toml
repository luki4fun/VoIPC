[package]
name = "voipc-protocol"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { workspace = true }
postcard = { workspace = true }
thiserror = { workspace = true }
bytes = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
# ProximityMode's serde names are the JSON contract for channels.json and the
# JS bridge; only the tests need a JSON codec.
serde_json = "1.0"
